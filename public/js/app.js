/* This function triggers when the date the task is supposed to be done. Then the function is triggered and deletes the item in question */
window.onload=function(){
    document.getElementById("timesOut").click();
}